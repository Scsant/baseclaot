const COLUMNS = [["nome", "Projeto"], ["caminhoes", "Caminhões"], ["volume_previsto", "Volume"], ["localizacao", "Localização"], ["gruas", "Gruas"], ["composicao", "Composição"], ["dmt_km", "DMT (km)"], ["viagens_dia", "Viagens/dia"], ["maximo_caminhoes", "Máx. caminhões"], ["classificacao", "Classificação"], ["produtividade_m3_h_grua", "Produtividade"], ["despacho_hora", "Despacho/h"]];

export default function TabelasOperacao({ projetos = [], transportadoras = [] }) {
  return (
    <div className="space-y-4"><section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><h2 className="text-lg font-bold">Dados por Projeto</h2><div className="mt-4 overflow-x-auto"><table className="min-w-[1200px] text-left text-xs"><thead><tr className="border-b border-border text-muted-foreground">{COLUMNS.map(([key, label]) => <th key={key} className="pb-3 pr-5">{label}</th>)}</tr></thead><tbody>{projetos.map((projeto, index) => <tr key={`${projeto.nome}-${index}`} className="border-b border-border/60">{COLUMNS.map(([key]) => <td key={key} className="py-3 pr-5 font-medium">{projeto[key] ?? "—"}</td>)}</tr>)}</tbody></table></div></section>
      <section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><h2 className="text-lg font-bold">Transportadoras</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead><tr className="border-b border-border text-xs text-muted-foreground"><th className="pb-3">Nome</th><th className="pb-3">Caminhões</th><th className="pb-3">Utilização</th><th className="pb-3">Distribuição entre projetos</th></tr></thead><tbody>{transportadoras.map((item, index) => <tr key={`${item.nome}-${index}`} className="border-b border-border/60"><td className="py-3 font-bold">{item.nome}</td><td>{item.caminhoes ?? "—"}</td><td>{item.percentual_utilizado != null ? `${item.percentual_utilizado}%` : "—"}</td><td className="text-muted-foreground">{item.distribuicao_projetos || "—"}</td></tr>)}</tbody></table></div></section></div>
  );
}